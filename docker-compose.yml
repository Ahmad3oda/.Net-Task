version: '3.8'

services:
  taskmanagement-redis:
    image: redis:latest
    container_name: taskmanagement-redis
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis_data:/data

volumes:
  redis_data:
